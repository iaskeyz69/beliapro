# Server-only key — NOT exposed in the website source code
# Vercel: Settings → Environment Variables → name: WEB3FORMS_ACCESS_KEY → Redeploy
# Local testing with API route: use `vercel dev` (not plain `npm run dev`)
WEB3FORMS_ACCESS_KEY=your_access_key_here
